/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  // preflight (reset) BẬT — chuẩn hoá margin/padding mặc định.
  theme: { extend: {} },
  plugins: [],
};
